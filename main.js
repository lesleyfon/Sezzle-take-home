const all_numbers = document.querySelectorAll("[data-integer]");
const operand = document.querySelectorAll("[data-operand]");
const clear_all = document.querySelector("[data-clear]");
const equal = document.querySelector("[data-equals]");
const integer_sign = document.querySelectorAll("[data-int-sign]");
